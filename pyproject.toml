[project]
name = "my-telegram-apple-music-downloader"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "gamdl===3.7.3",
    "python-decouple>=3.8",
    "python-telegram-bot[job-queue,webhooks]>=22.2",
]
